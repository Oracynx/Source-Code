#include <cmath>
#include <cstdio>
constexpr int MaxN = 3e5 + 5;
int n, m, b;
int p[MaxN];
int main()
{
    scanf("%d%d", &n, &m);
    for (int i = 1; i <= 1 << n; i++)
    {
    }
    b = std::sqrt((1 << n) * n);
    return 0;
}